commands:
  01_clean_tmp:
    command: |
      # Clean up temporary files before installation
      rm -rf /tmp/pip*
      rm -rf /tmp/*.log
      find /tmp -name "*.pyc" -delete
    ignoreErrors: true