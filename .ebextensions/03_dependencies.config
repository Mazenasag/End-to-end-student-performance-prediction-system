container_commands:
  01_optimize_pip_install:
    command: |
      # Use pip cache and avoid building wheels when possible
      pip install --no-cache-dir --no-build-isolation -r requirements.txt
    leader_only: true